import 'package:flutter/material.dart';

class PlyObjectPage extends StatelessWidget {
  const PlyObjectPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
